"use client";
import { useRef } from "react";
import Link from "next/link";
import { ArrowUpRight, ArrowDown } from "lucide-react";
export function Hero(){const art=useRef<HTMLDivElement>(null);return <section className="hero container"><div className="hero-copy"><p className="eyebrow">쇼핑몰·서비스업 대표님을 위한 일대일 AI 코칭</p><h1>상세페이지 고치고,<br/>블로그 쓰면서<br/><span className="underline-lemon">AI를 배웁니다.</span></h1><p className="hero-intro">대표님이 운영하는 사이트를 같이 보며 진행합니다.<br className="desktop-break"/> 상품 설명을 쓰고, 글과 영상을 만드는 데 AI를 써봅니다.<br className="desktop-break"/> 혼자서도 다시 해보실 수 있도록 작업 순서까지 알려드립니다.</p><Link href="/apply" className="button button-coral">1시간 무료 상담 신청 <ArrowUpRight size={20}/></Link><p className="hero-note">Zoom으로 1시간 상담 · 코칭 신청은 상담 후 결정</p></div><div className="hero-art" ref={art} onPointerMove={e=>{if(e.pointerType==='touch'||matchMedia('(prefers-reduced-motion: reduce)').matches)return;const b=e.currentTarget.getBoundingClientRect();art.current?.style.setProperty('--px',`${((e.clientX-b.left)/b.width-.5)*18}px`);art.current?.style.setProperty('--py',`${((e.clientY-b.top)/b.height-.5)*14}px`);}} onPointerLeave={()=>{art.current?.style.setProperty('--px','0px');art.current?.style.setProperty('--py','0px')}}><img src="/images/coaching-session.webp" width="1448" height="1086" alt="두 사람이 노트북과 작업 자료를 함께 살펴보는 코칭 일러스트" fetchPriority="high"/><span className="art-label label-top">사이트를 같이 보면서</span><span className="art-label label-bottom">한 단계씩 따라 해보세요</span></div><a href="#coaching" className="hero-scroll"><ArrowDown size={16}/> 코칭 내용 살펴보기</a></section>}
