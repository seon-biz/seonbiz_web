"use client";
import { useRef } from "react";
import Link from "next/link";
import { ArrowUpRight, ArrowDown } from "lucide-react";
export function Hero(){const art=useRef<HTMLDivElement>(null);return <section className="hero container"><div className="hero-copy"><p className="eyebrow">쇼핑몰·서비스 사업자를 위한 AI 1:1 코칭</p><h1>열심히 알리는데,<br/>고객은 왜 <span className="underline-lemon">망설일까요?</span></h1><p className="hero-intro">상세페이지를 고쳐도 주문이 뜸하고,<br className="desktop-break"/> AI로 쓴 글은 우리 사업 이야기 같지 않다면.<br className="desktop-break"/> 대표님의 사이트에서 고칠 곳을 찾고,<br className="desktop-break"/> AI로 바꾸는 방법까지 1:1로 함께합니다.</p><Link href="/apply" className="button button-coral">1시간 무료 상담 신청 <ArrowUpRight size={20}/></Link><p className="hero-note">첫 상담 1시간 · Zoom · 유료 코칭은 상담 후 결정</p></div><div className="hero-art" ref={art} onPointerMove={e=>{if(e.pointerType==='touch'||matchMedia('(prefers-reduced-motion: reduce)').matches)return;const b=e.currentTarget.getBoundingClientRect();art.current?.style.setProperty('--px',`${((e.clientX-b.left)/b.width-.5)*18}px`);art.current?.style.setProperty('--py',`${((e.clientY-b.top)/b.height-.5)*14}px`);}} onPointerLeave={()=>{art.current?.style.setProperty('--px','0px');art.current?.style.setProperty('--py','0px')}}><img src="/images/coaching-session.webp" width="1448" height="1086" alt="두 사람이 노트북과 작업 자료를 함께 살펴보는 코칭 일러스트" fetchPriority="high"/><span className="art-label label-top">내 사이트를 함께 보며</span><span className="art-label label-bottom">무엇부터 고칠지 확인</span></div><a href="#coaching" className="hero-scroll"><ArrowDown size={16}/> 이런 고민을 함께 다룹니다</a></section>}
