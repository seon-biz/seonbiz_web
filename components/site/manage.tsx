"use client";
import { useState } from 'react';
import { Input } from '@/components/ui/input';
import { AlertDialog, AlertDialogTrigger, AlertDialogContent, AlertDialogHeader, AlertDialogTitle, AlertDialogDescription, AlertDialogFooter, AlertDialogCancel, AlertDialogAction } from '@/components/ui/alert-dialog';
export function ManageLogin(){const [error,setError]=useState('');const [busy,setBusy]=useState(false);return <form className="manage-login" onSubmit={async e=>{e.preventDefault();setBusy(true);setError('');const key=new FormData(e.currentTarget).get('key');try{const response=await fetch('/api/manage/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({key})});const data=await response.json() as {ok?:boolean;error?:string};if(!response.ok)throw new Error(data.error);location.reload()}catch(e){setError(e instanceof Error?e.message:'연결을 확인해 주세요.')}finally{setBusy(false)}}}><label htmlFor="key">관리자 접속키</label><Input id="key" name="key" type="password" required autoComplete="current-password"/>{error&&<p role="alert" className="form-error">{error}</p>}<button className="button button-ink" disabled={busy}>{busy?'확인 중':'접수 내역 확인'}</button></form>}
export function Logout(){return <button className="text-link" onClick={async()=>{const r=await fetch('/api/manage/logout',{method:'POST'});if(r.ok)location.reload()}}>로그아웃</button>}
export function DeleteApplication({id}:{id:string}){const [error,setError]=useState('');return <><AlertDialog><AlertDialogTrigger className="delete-record">상담 완료 · 정보 삭제</AlertDialogTrigger><AlertDialogContent><AlertDialogHeader><AlertDialogTitle>이 신청 정보를 삭제할까요?</AlertDialogTitle><AlertDialogDescription>상담 목적을 달성했거나 신청 철회 요청을 받은 경우 삭제하세요. 업종과 연락처, 상담 내용 등 이 신청 정보가 영구 삭제됩니다.</AlertDialogDescription></AlertDialogHeader><AlertDialogFooter><AlertDialogCancel>취소</AlertDialogCancel><AlertDialogAction onClick={async()=>{try{const r=await fetch(`/api/manage/applications/${id}`,{method:'DELETE'});if(!r.ok)throw new Error();location.reload()}catch{setError('삭제되지 않았습니다. 다시 시도해 주세요.')}}}>정보 삭제</AlertDialogAction></AlertDialogFooter></AlertDialogContent></AlertDialog>{error&&<p role="alert" className="form-error">{error}</p>}</>}
