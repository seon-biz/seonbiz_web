"use client";
import { useRef } from "react";
import { ArrowUpRight, ArrowDown } from "lucide-react";
export function Hero(){const art=useRef<HTMLDivElement>(null);return <section className="hero container"><div className="hero-copy"><p className="eyebrow">홈페이지를 직접 만들고 관리하는 일대일 AI 코칭</p><h1>내 사업에 맞는 홈페이지를<br/><span className="underline-lemon">AI로 직접 만듭니다.</span></h1><p className="hero-intro">사업과 서비스 정보를 홈페이지에 정확하게 정리합니다.<br/>검색과 AI 서비스가 이해하고 추천할 수 있는 기반을 만들되, 특정 노출이나 추천을 보장하지는 않습니다.</p><a href="/apply" className="button button-coral">1시간 무료 상담 신청 <ArrowUpRight size={20}/></a><p className="hero-note">Zoom으로 1시간 상담 · 코칭 신청은 상담 후 결정</p></div><div className="hero-art" ref={art} onPointerMove={e=>{if(e.pointerType==='touch'||matchMedia('(prefers-reduced-motion: reduce)').matches)return;const b=e.currentTarget.getBoundingClientRect();art.current?.style.setProperty('--px',`${((e.clientX-b.left)/b.width-.5)*18}px`);art.current?.style.setProperty('--py',`${((e.clientY-b.top)/b.height-.5)*14}px`);}} onPointerLeave={()=>{art.current?.style.setProperty('--px','0px');art.current?.style.setProperty('--py','0px')}}><img src="/images/service-website.webp" width="1447" height="1087" alt="사업 자료와 현장 사진을 홈페이지의 서비스 소개와 문의 안내로 정리하는 일러스트" fetchPriority="high" decoding="async"/><span className="art-label label-top">사업의 실제 정보로</span><span className="art-label label-bottom">직접 관리하는 홈페이지까지</span></div><a href="#coaching" className="hero-scroll"><ArrowDown size={16}/> 코칭 내용 살펴보기</a></section>}
