import { ArrowUpRight } from "lucide-react";
import { caseSummaries } from "@/lib/content";
import { ConsultCTA } from "@/components/site/coaching-blocks";
export const metadata={title:'실제 사례',description:'세온비즈가 진행한 식품 쇼핑몰 작업과 자체 콘텐츠 제작 사례입니다. 구글 검색 기록과 실제 결과물을 소개합니다.'};
export default function Cases(){return <main id="main"><section className="inner-hero container"><p className="eyebrow">실제 사례</p><h1>바꾼 내용과 결과를<br/>실제 자료로 확인하세요.</h1><p className="lead">상품 정보를 정비한 고객 사례와 직접 만든 콘텐츠를 소개합니다.<br/>작업 내용, 비교 기간, 숫자를 해석할 때 알아둘 점을 함께 담았습니다.</p></section><section className="container case-grid">{caseSummaries.map(item=><a key={item.slug} href={`/cases/${item.slug}`} className={`case-card ${item.slug==='content-workflow'?'case-content':''}`}><div className="case-thumb"><img src={item.image} alt={item.alt} loading="lazy"/></div><div className="case-card-copy"><p className="kicker">{item.tag}</p><h2>{item.title.replace('\n',' ')}</h2><p>{item.summary}</p><span className="text-link">작업 내용과 자료 보기 <ArrowUpRight size={18}/></span></div></a>)}</section><ConsultCTA/></main>}
