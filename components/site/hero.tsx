"use client";
import { useRef } from "react";
import { ArrowUpRight, ArrowDown } from "lucide-react";
export function Hero(){const art=useRef<HTMLDivElement>(null);return <section className="hero container"><div className="hero-copy"><p className="eyebrow">홈페이지를 직접 만들고 고치려는 대표님을 위한 일대일 AI 코칭</p><h1>두 시간 뒤,<br/>대표님이 <span className="underline-lemon">직접 고치고</span><br/>계십니다.</h1><p className="hero-intro">Zoom으로 함께 만들고, 도메인·계정·수정 방법을 전부 대표님께 남깁니다.<br/>문구 하나 바꾸려고 제작 업체에 연락하고 며칠씩 기다리지 않습니다.</p><div className="hero-disclaimer-space" aria-hidden="true"/><a href="/apply" className="button button-coral">1시간 무료 상담 신청 <ArrowUpRight size={20}/></a><p className="hero-note">신청 후 1영업일 이내 카카오톡으로 연락드립니다. 신청만으로 결제되지 않습니다.</p></div><div className="hero-art" ref={art} onPointerMove={e=>{if(e.pointerType==='touch'||matchMedia('(prefers-reduced-motion: reduce)').matches)return;const b=e.currentTarget.getBoundingClientRect();art.current?.style.setProperty('--px',`${((e.clientX-b.left)/b.width-.5)*18}px`);art.current?.style.setProperty('--py',`${((e.clientY-b.top)/b.height-.5)*14}px`);}} onPointerLeave={()=>{art.current?.style.setProperty('--px','0px');art.current?.style.setProperty('--py','0px')}}><img src="/images/service-website.webp" width="1447" height="1087" alt="사업 자료와 현장 사진을 홈페이지의 서비스 소개와 문의 안내로 정리하는 일러스트" fetchPriority="high" decoding="async"/><span className="art-label label-top">대표님 계정에서 작업하고</span><span className="art-label label-bottom">계정과 수정 권한은 대표님께 남습니다</span></div><a href="#coaching" className="hero-scroll"><ArrowDown size={16}/> 코칭 내용 살펴보기</a></section>}
