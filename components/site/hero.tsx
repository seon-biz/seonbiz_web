"use client";
import { useRef } from "react";
import Link from "next/link";
import { ArrowUpRight, ArrowDown } from "lucide-react";
export function Hero(){const art=useRef<HTMLDivElement>(null);return <section className="hero container"><div className="hero-copy"><p className="eyebrow">사업자를 위한 AI 실무 1:1 코칭</p><h1>막힌 일은 함께.<br/>다음 일은 <span className="underline-lemon">직접.</span></h1><p className="hero-intro">실제 사업의 화면과 자료를 펼쳐놓고,<br className="desktop-break"/> 지금 바꿀 일을 함께 정하고 실행합니다.<br className="desktop-break"/> 다음 작업은 스스로 이어갈 수 있도록.</p><Link href="/apply" className="button button-coral">1시간 무료 상담 신청 <ArrowUpRight size={20}/></Link><p className="hero-note">Zoom 화면 공유 · 공동 실습 · 요약과 다음 과제</p></div><div className="hero-art" ref={art} onPointerMove={e=>{if(e.pointerType==='touch'||matchMedia('(prefers-reduced-motion: reduce)').matches)return;const b=e.currentTarget.getBoundingClientRect();art.current?.style.setProperty('--px',`${((e.clientX-b.left)/b.width-.5)*18}px`);art.current?.style.setProperty('--py',`${((e.clientY-b.top)/b.height-.5)*14}px`);}} onPointerLeave={()=>{art.current?.style.setProperty('--px','0px');art.current?.style.setProperty('--py','0px')}}><img src="/images/coaching-session.webp" width="1448" height="1086" alt="두 사람이 노트북과 작업 자료를 함께 살펴보는 코칭 일러스트" fetchPriority="high"/><span className="art-label label-top">막히는 화면부터</span><span className="art-label label-bottom">같이 보고, 함께 판단</span></div><a href="#coaching" className="hero-scroll"><ArrowDown size={16}/> 우리 사업에서 시작할 일</a></section>}
